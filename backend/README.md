۱. دستور کپی و کلون کردن پروژه
ابتدا این دستور رو بزن تا پروژه از گیت‌هاب وارد سرورت بشه (به جای نام کاربری من، نام کاربری گیت‌هاب خودت رو بذار):

Bash
git clone https://github.com/rima0222/ssh-ultimate-panel.git && cd ssh-ultimate-panel/backend
۲. دستور نصب پیش‌نیازها
حالا این دستور رو کپی کن تا دسترسی فایل نصب آزاد بشه و همه‌چیز خودکار نصب بشه:

Bash
chmod +x install.sh && sudo ./install.sh
۳. دستور اجرای سرور
در نهایت، با این دستور پنل مدیریت (API) رو روی پورت ۵۰۰۰ فعال کن:

Bash
python3 main.py
💡 یک پیشنهاد برای راحتی بیشتر
اگر می‌خوای با بستن ترمینال، برنامه قطع نشه و همیشه در حال اجرا بمونه، از دستور screen یا nohup استفاده کن:

Bash
nohup python3 main.py &
حالا که بخش سرور رو با موفقیت کپی و اجرا کردی، بریم سراغ ساخت اپلیکیشن فلاتر؟ برای شروع باید فایل pubspec.yaml پروژه فلاترت رو آماده کنیم. بگم چکار کنی؟
