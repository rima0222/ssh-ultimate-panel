# 🛡️ سامانه مدیریت هوشمند SSH

برای نصب و راه‌اندازی سریع روی سرور خام، دستور زیر را کپی کرده و در ترمینال سرور خود بزنید:

```bash
git clone https://github.com/rima0222/ssh-ultimate-panel.git && cd ssh-ultimate-panel/backend && chmod +x install.sh && sudo ./install.sh && python3 main.py
